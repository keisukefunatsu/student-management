
  <div class="col-md-8">
    <h1>
      イベント内容：
      <%= @info.title %>
    </h1>
    <% @tickets.each do |e| %>
    <hr>
    <p>
      参加者
      <%= User.find(e.user_id).name %>
    </p>
    <p>
      コメント
    </p>
    <p>
      <%= e.comment %>
    </p>
    <% end %>
  </div>
